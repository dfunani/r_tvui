# LinkedIn post drafts — R-TVUI v3.1.0

Pick one, tweak voice, add a screenshot or short screen recording if you can.

---

## Option A — punchy (recommended)

Your terminal deserves a real file explorer — not another `cd` loop.

I shipped **R-TVUI v3.1.0**: a keyboard-first TUI file manager in **Rust**.

Browse folders. Preview files. Mark, move, delete (with confirm + trash). Tabs. Split panes. Git hints in the list. One small binary — no Electron, no drama.

Try it:
https://r-tvui-web.vercel.app/download

Built in public. MIT. Feedback welcome.

#Rust #OpenSource #CLI #DeveloperTools #Terminal

---

## Option B — story-led

I got tired of bouncing between `ls`, `cd`, and GUI Finder just to move a few files in the terminal.

So I built **R-TVUI** — a fast TUI file explorer in Rust.

v3.1.0 is out today:
→ live preview & image summaries  
→ multi-tab + dual-pane split  
→ bookmarks, history, git status column  
→ macOS, Linux, Windows builds  

One command install:
https://r-tvui-web.vercel.app/download

If you live in the terminal, I'd love your take.

#RustLang #TUI #DevTools

---

## Option C — technical credibility

**R-TVUI v3.1.0** — terminal file explorer in Rust (ratatui + async I/O)

What shipped:
• Async directory listing (50k cap) + text/image preview  
• Tabs, split cwd, visual marks, pane copy/move  
• Trash-safe delete, `$EDITOR`, git porcelain column  
• CI-built releases for darwin / linux / windows  

Download: https://r-tvui-web.vercel.app/download  
Repo: https://github.com/dfunani/r_tvui

Small binary. Keyboard-first. MIT.

---

## Tips for the post

1. Attach a **15–30s screen recording** (navigate → preview → split pane) — highest engagement.
2. Pin the **download link** in the first comment if LinkedIn truncates URLs.
3. Tag nothing controversial; `#Rust` + `#DeveloperTools` is enough.
4. Reply to every comment in the first hour — algorithm boost.
